name:santosh
on:
  push:
     branches: [main]
jobs:
  greet: 
    runs-on: ubuntu
    steps: 
      - name: say heloo to users
    run: echo "hello Dosto"
     - name: say hello to username
    run: echo "Hello $USER" 
  
